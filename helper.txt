if some file is pushed to git, after then you need to ignore it from git, then 

git rm -r --cached path/to/your/file.json
git add .gitignore
git commit -m "Ignore json files"


added ssh key for verifing commits using cryptography